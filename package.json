{
  "name": "khata",
  "version": "1.0.0",
  "description": "A comprehensive mobile-first application for managing customers, suppliers, daily transactions (Day Book), and inventory (Stock Book). It offers features for tracking accounts, stock levels, and generating reports.",
  "private": true,
  "scripts": {
    "build": "echo \"This application is configured to run directly in the browser without a build step.\""
  },
  "dependencies": {},
  "devDependencies": {}
}
